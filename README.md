# Stage 1 is all about Preprocessing and datahandling 

`Input Thermal Image
→ Add Artificial Damage
→ Create Mask(valid pixel mask, missing pixel locations.)
→ Apply OpenCV Inpainting(autoEncoder)
→ Show Restored Result`